# КАДРЫ — Система управления персоналом

Веб-приложение для отдела кадров, предназначенное для работы в локальной сети компании.

## 🚀 Быстрый старт (Docker)

```bash
# Клонировать репозиторий
git clone <repo-url>
cd HR

# Запустить все сервисы
docker-compose up -d --build

# Подождать ~30 секунд пока запустится БД, затем заполнить данными
docker cp backend/prisma/seed.js hrms_backend:/app/prisma/seed.js
docker-compose exec backend node prisma/seed.js

# Приложение доступно на http://localhost:7474
```

## 🌐 Доступ к приложению

**URL:** http://localhost:7474

### 🔑 Тестовые учётные записи

| Email | Пароль | Роль | Права |
|-------|--------|------|-------|
| `hr@example.com` | `password` | Кадровик | Полный доступ |
| `manager@example.com` | `password` | Руководитель | Просмотр сотрудников |
| `accountant@example.com` | `password` | Бухгалтер | Просмотр сотрудников |

### 📊 Тестовые данные

После запуска seed-скрипта в базе будут:
- **5 отделов:** IT-отдел, HR-отдел, Бухгалтерия, Маркетинг, Отдел продаж
- **8 должностей:** Разработчик, Менеджер, HR-специалист, Бухгалтер, Маркетолог, Тестировщик, Дизайнер, Руководитель отдела
- **5 тестовых сотрудников** с заполненными профилями
- **3 пользователя** с разными ролями

## 🐳 Docker сервисы

| Сервис | Контейнер | Внутренний порт | Внешний порт |
|--------|-----------|-----------------|--------------|
| MySQL 8.0 | hrms_db | 3306 | 7171 |
| Backend (NestJS) | hrms_backend | 7070 | 7272 |
| Frontend (Next.js) | hrms_frontend | 7070 | 7373 |
| Nginx (прокси) | hrms_nginx | 80 | **7474** |

## 📝 Полезные команды

```bash
# Запустить все сервисы
docker-compose up -d

# Остановить все сервисы
docker-compose down

# Посмотреть логи всех сервисов
docker-compose logs -f

# Посмотреть логи конкретного сервиса
docker-compose logs -f backend
docker-compose logs -f frontend

# Пересоздать с чистой базой данных
docker-compose down -v
docker-compose up -d --build

# Выполнить миграции вручную
docker-compose exec backend npx prisma migrate deploy

# Заполнить базу тестовыми данными
docker cp backend/prisma/seed.js hrms_backend:/app/prisma/seed.js
docker-compose exec backend node prisma/seed.js

# Открыть Prisma Studio (GUI для БД)
docker-compose exec backend npx prisma studio

# Подключиться к MySQL
docker-compose exec db mysql -u hrms -phrmspassword123 hrms

# Перезапустить конкретный сервис
docker-compose restart backend
```

## 💻 Локальная разработка (без Docker)

### Требования
- Node.js 20+
- MySQL 8.0
- npm

### 1. База данных

```bash
# Создайте базу данных MySQL
mysql -u root -p
```

```sql
CREATE DATABASE hrms;
CREATE USER 'hrms'@'localhost' IDENTIFIED BY 'hrmspassword123';
GRANT ALL PRIVILEGES ON hrms.* TO 'hrms'@'localhost';
FLUSH PRIVILEGES;
```

### 2. Backend

```bash
cd backend

# Установить зависимости
npm install

# Создать .env файл
cp .env.example .env
# Отредактируйте DATABASE_URL: mysql://hrms:hrmspassword123@localhost:3306/hrms

# Применить миграции
npm run db:push

# Заполнить тестовыми данными
npm run db:seed

# Запустить в режиме разработки
npm run start:dev
```

### 3. Frontend

```bash
cd frontend

# Установить зависимости
npm install

# Создать .env.local
echo "NEXT_PUBLIC_API_URL=http://localhost:3000/api" > .env.local

# Запустить в режиме разработки
npm run dev
```

**Приложение будет доступно:**
- Frontend: http://localhost:3000
- Backend API: http://localhost:3000/api

## ✨ Функциональность

### Реализовано
- ✅ **Авторизация** — JWT токены, bcrypt хеширование паролей
- ✅ **Роли и права (RBAC)** — 4 роли с разным уровнем доступа
- ✅ **Сотрудники** — полный CRUD, поиск, пагинация
- ✅ **Отделы** — создание, редактирование, удаление
- ✅ **Должности** — создание, редактирование, удаление
- ✅ **Документы** — загрузка, скачивание файлов (PDF, JPEG, PNG, DOC, DOCX)
- ✅ **Профили сотрудников** — детальная информация, документы
- ✅ **Дашборд** — статистика по персоналу
- ✅ **Полностью на русском языке**

### Матрица прав доступа

| Функция | Кадровик | Руководитель | Бухгалтер | Сотрудник |
|---------|:--------:|:------------:|:---------:|:---------:|
| Просмотр списка сотрудников | ✅ | ✅ | ❌ | ❌ |
| Просмотр профиля сотрудника | ✅ | ✅ | ✅ | ❌ |
| Создание сотрудников | ✅ | ❌ | ❌ | ❌ |
| Редактирование сотрудников | ✅ | ❌ | ❌ | ❌ |
| Удаление сотрудников | ✅ | ❌ | ❌ | ❌ |
| Управление отделами | ✅ | ❌ | ❌ | ❌ |
| Управление должностями | ✅ | ❌ | ❌ | ❌ |
| Загрузка документов | ✅ | ❌ | ❌ | ❌ |
| Скачивание документов | ✅ | ✅ | ✅ | ❌ |

## 🛠 Технологии

| Компонент | Технология | Версия |
|-----------|------------|--------|
| **Backend** | NestJS + TypeScript | 11.x |
| **Frontend** | Next.js + React | 15.x / 19.x |
| **База данных** | MySQL | 8.0 |
| **ORM** | Prisma | 5.x |
| **UI** | Tailwind CSS + shadcn/ui | 4.x |
| **Авторизация** | JWT + Passport.js | — |
| **Контейнеризация** | Docker + docker-compose | — |
| **Прокси** | Nginx | 1.27 |

## 📁 Структура проекта

```
HR/
├── backend/                    # NestJS API
│   ├── src/
│   │   ├── auth/              # Авторизация (JWT, Passport)
│   │   ├── employees/         # CRUD сотрудников
│   │   ├── departments/       # CRUD отделов
│   │   ├── positions/         # CRUD должностей
│   │   ├── documents/         # Загрузка/скачивание файлов
│   │   ├── users/             # Управление пользователями
│   │   └── prisma/            # Prisma сервис
│   └── prisma/
│       ├── schema.prisma      # Схема базы данных
│       ├── seed.ts            # Тестовые данные (TypeScript)
│       ├── seed.js            # Тестовые данные (JavaScript)
│       └── migrations/        # Миграции БД
├── frontend/                   # Next.js приложение
│   ├── app/
│   │   ├── (app)/             # Защищённые страницы
│   │   │   ├── dashboard/     # Главная панель
│   │   │   ├── employees/     # Сотрудники
│   │   │   ├── departments/   # Отделы
│   │   │   ├── positions/     # Должности
│   │   │   ├── reports/       # Отчёты
│   │   │   └── settings/      # Настройки
│   │   ├── (auth)/            # Страницы авторизации
│   │   │   └── login/         # Вход
│   │   └── contexts/          # React контексты
│   ├── components/            # UI компоненты
│   │   └── ui/                # shadcn/ui компоненты
│   └── lib/                   # Утилиты и API клиент
├── storage/                    # Хранилище документов (монтируется в Docker)
│   ├── employees/             # Папки сотрудников
│   │   └── {FirstName}_{LastName}_{id}/
│   │       └── docs/          # Документы сотрудника
│   └── tmp/                   # Временные файлы при загрузке
├── docker/
│   └── nginx/
│       └── default.conf       # Конфигурация Nginx
├── docker-compose.yml         # Docker оркестрация
├── .env                       # Переменные окружения
├── .env.example               # Пример переменных
└── README.md                  # Документация
```

## 📂 Хранилище документов

При создании сотрудника автоматически создаётся персональная папка для документов:

```
storage/employees/{FirstName}_{LastName}_{id}/docs/
```

**Пример:**
```
storage/employees/Ivan_Petrov_1/docs/
├── passport_2026-01-30.pdf
├── snils_2026-01-30.pdf
└── employment_contract_2026-01-30.pdf
```

Папка `storage/` примонтирована из Docker-контейнера в корень проекта, поэтому все документы доступны на хост-машине.

## 🔧 API Endpoints

### Авторизация
```
POST /api/auth/login     — Вход (email, password) → access_token
POST /api/auth/register  — Регистрация нового пользователя
```

### Сотрудники (требуется JWT)
```
GET    /api/employees           — Список (page, limit, search)
GET    /api/employees/:id       — Детали сотрудника
POST   /api/employees           — Создать (только Кадровик)
PATCH  /api/employees/:id       — Обновить (только Кадровик)
DELETE /api/employees/:id       — Удалить (только Кадровик)
```

### Отделы (требуется JWT)
```
GET    /api/departments         — Список всех отделов
GET    /api/departments/:id     — Детали отдела
POST   /api/departments         — Создать (только Кадровик)
PATCH  /api/departments/:id     — Обновить (только Кадровик)
DELETE /api/departments/:id     — Удалить (только Кадровик)
```

### Должности (требуется JWT)
```
GET    /api/positions           — Список всех должностей
GET    /api/positions/:id       — Детали должности
POST   /api/positions           — Создать (только Кадровик)
PATCH  /api/positions/:id       — Обновить (только Кадровик)
DELETE /api/positions/:id       — Удалить (только Кадровик)
```

### Документы (требуется JWT)
```
GET  /api/documents/employee/:id           — Список документов сотрудника
POST /api/documents/upload/employee/:id    — Загрузить документ (multipart/form-data)
GET  /api/documents/:id/download           — Скачать документ
```

## 📝 Переменные окружения

### Корневой .env (для Docker)
```env
# База данных MySQL
MYSQL_USER=hrms
MYSQL_PASSWORD=hrmspassword123
MYSQL_ROOT_PASSWORD=rootpassword123
MYSQL_DATABASE=hrms
DATABASE_URL="mysql://hrms:hrmspassword123@db:3306/hrms"

# JWT
JWT_SECRET=hrms-jwt-secret-key-2024-very-secure

# Порты
PORT=7070
NEXT_PUBLIC_API_URL=/api
```

### backend/.env (для локальной разработки)
```env
DATABASE_URL="mysql://hrms:hrmspassword123@localhost:3306/hrms"
JWT_SECRET=hrms-jwt-secret-key-2024-very-secure
PORT=3000
```

### frontend/.env.local (для локальной разработки)
```env
NEXT_PUBLIC_API_URL=http://localhost:3000/api
```

## 🔒 Безопасность

- Пароли хешируются с bcrypt (10 rounds)
- JWT токены с временем жизни 60 минут
- CORS настроен для разрешённых доменов
- Валидация всех входных данных (class-validator)
- Роли проверяются на каждом защищённом эндпоинте
- Файлы хранятся вне публичной директории

## 🐛 Решение проблем

### Контейнер backend не запускается
```bash
# Проверьте логи
docker-compose logs backend

# Убедитесь что БД готова
docker-compose logs db
```

### Ошибка подключения к БД
```bash
# Пересоздайте volume
docker-compose down -v
docker-compose up -d
```

### Frontend не видит API
```bash
# Проверьте что backend работает
curl http://localhost:7272/api

# Проверьте nginx
curl http://localhost:7474/api/auth/login -X POST \
  -H "Content-Type: application/json" \
  -d '{"email":"hr@example.com","password":"password"}'
```

## 📄 Лицензия

MIT
