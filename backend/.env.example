# Backend .env (для локальной разработки без Docker)
# Для Docker используйте корневой .env файл

# Подключение к БД
# Локальная разработка:
DATABASE_URL="mysql://hrms:hrmspassword123@localhost:7171/hrms"
# Docker (не менять):
# DATABASE_URL="mysql://hrms:hrmspassword123@db:3306/hrms"

# JWT секрет
JWT_SECRET=hrms-jwt-secret-key-2024-very-secure

# Порт сервера
PORT=3000
